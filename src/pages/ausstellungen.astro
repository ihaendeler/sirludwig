---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageHero from '../components/PageHero.astro';
import { getCanonical } from '../config/site';

const path = '/ausstellungen';
const placeholderIntro =
	'Dieser Bereich ist vorbereitet und wird in einer späteren Phase ausgearbeitet.';
---

<BaseLayout
	title="Ausstellungen — Platzhalter"
	description="Bereich Ausstellungen — in Vorbereitung."
	canonical={getCanonical(path)}
>
	<PageHero
		label="Bereich"
		title="Ausstellungen"
		intro={placeholderIntro}
		headingId="page-heading"
	/>
</BaseLayout>
