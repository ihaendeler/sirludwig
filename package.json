{
  "name": "sirludwig",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "generate:og": "tsx src/scripts/generate-og.ts",
    "generate:icons": "tsx src/scripts/generate-favicons.ts",
    "prebuild": "npm run generate:og && npm run generate:icons",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "astro": "^6.4.7"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "to-ico": "^1.1.5",
    "tsx": "^4.21.0"
  }
}
